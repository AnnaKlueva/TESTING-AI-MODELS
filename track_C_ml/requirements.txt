# Трек C (ML). Класичний sklearn-стек, без GPU і ключів.
# УВАГА: sut_artifact.joblib — pickle; для завантаження без попереджень тримай версії
# scikit-learn/numpy близькими до тих, на яких артефакт зібрано (зафіксуй їх у test_strategy.md).
pytest>=8.0
scikit-learn>=1.3
pandas>=2.0
numpy>=1.24
joblib>=1.3
matplotlib>=3.7
# evidently>=0.4       # для перевірки дрейфу; можна і без нього
